import { Test } from "jest-runner";
import { Config } from "@jest/types";
import { TestResult } from "@jest/test-result";
export declare class JazzerWorker {
    #private;
    readonly defaultTimeout = 5000;
    constructor();
    static get currentTestPath(): string;
    private initialize;
    private static setupGlobal;
    run(test: Test, config: Config.GlobalConfig): Promise<TestResult>;
    private loadTests;
    private runDescribeBlock;
    private runTestEntry;
    private runHooks;
    private runHook;
    private executeHook;
    private testResult;
    private failureToString;
    /**
     *  If we always remove the dollar sign, then the runner will run all tests matching to a test name.
     *  For that reason, we only remove the dollar sign if the test name matches exactly.
     */
    private adjustTestPattern;
    private shouldSkipTest;
    private fullTestPath;
    private doesMatch;
    private importFile;
}
