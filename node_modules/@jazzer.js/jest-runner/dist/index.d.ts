import { FuzzTest } from "./fuzz";
import { CallbackTestRunner, OnTestFailure, OnTestStart, OnTestSuccess, Test, TestRunnerContext, TestRunnerOptions, TestWatcher } from "jest-runner";
import { Config } from "@jest/types";
declare class FuzzRunner extends CallbackTestRunner {
    #private;
    shouldCollectCoverage: boolean;
    coverageReporters: Config.CoverageReporters;
    constructor(globalConfig: Config.GlobalConfig, context: TestRunnerContext);
    runTests(tests: Array<Test>, watcher: TestWatcher, onStart: OnTestStart, onResult: OnTestSuccess, onFailure: OnTestFailure, options: TestRunnerOptions): Promise<void>;
}
export default FuzzRunner;
declare global {
    namespace jest {
        interface It {
            fuzz: FuzzTest;
        }
    }
}
